{
  "name": "timos-agent",
  "version": "0.1.0",
  "description": "Local coding worker router — register workspaces, workers, and local jobs",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "agent": "node scripts/agent.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
